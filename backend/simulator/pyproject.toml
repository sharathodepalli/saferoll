[project]
name = "saferoll-simulator"
version = "0.1.0"
description = "Device simulator for SafeRoll"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.28",
  "typer>=0.12",
]

[project.scripts]
saferoll-sim = "simulator.cli:app"
